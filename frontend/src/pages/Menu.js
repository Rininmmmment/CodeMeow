import React from 'react';

const Menu = () => {
  return (
    <div>
        <a href="#">MAKE AND WDIT QUIZ</a>
        <a href="#">PLAY CODE</a>
        <a href="#">SETTINGS</a>
    </div>
  );
};

export default Menu;